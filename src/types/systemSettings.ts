export type LanguageCode='en'|'zh'|'ms';
export interface RestaurantInfo{restaurantName:string;legalCompanyName:string;registrationNumber:string;taxRegistrationNumber:string;phoneNumber:string;email:string;website:string;addressLine1:string;addressLine2:string;city:string;state:string;postcode:string;country:string;branchName:string;branchCode:string}
export interface PrinterConfig{id:string;name:string;type:'RECEIPT'|'KITCHEN'|'BEVERAGE'|'OTHER';connectionType:'NETWORK'|'USB'|'BLUETOOTH'|'SYSTEM_PRINT';ipAddress:string;port:string;paperWidth:58|80;autoCut:boolean;copies:number;enabled:boolean}
export interface KitchenStation{id:string;name:string;code:string;stationType:'MAIN_KITCHEN'|'BEVERAGE'|'DESSERT'|'BAR'|'OTHER';printerId:string;kdsDeviceKey:string;categoryIds:string[];enabled:boolean}
export interface NumberingSetting{entityCode:'ORD'|'PAY'|'RCP'|'REF';prefix:string;sequencePadding:number;dateFormat:'YYYYMMDD'|'YYMMDD'|'YYYY-MM';resetFrequency:'NEVER'|'DAILY'|'MONTHLY'|'YEARLY'}
export interface IntegrationConfig{integrationType:string;provider:string;enabled:boolean;status:'CONNECTED'|'NOT_CONFIGURED'|'DISCONNECTED'|'ERROR';publicConfig:Record<string,unknown>;credentialsConfigured:boolean}
export interface SystemSettings{revision:number;canEdit:boolean;restaurantInfo:RestaurantInfo;logoPath:string;logoUrl:string;taxEnabled:boolean;taxName:string;taxRate:number;taxMode:'INCLUSIVE'|'EXCLUSIVE';serviceChargeEnabled:boolean;serviceChargeName:string;serviceChargeRate:number;serviceChargeOrderTypes:string[];receiptConfig:Record<string,unknown>;timezone:string;currencyCode:string;currencySymbol:string;decimalPlaces:number;roundingRule:string;defaultLanguage:LanguageCode;enabledLanguages:LanguageCode[];backupConfig:Record<string,unknown>;printers:PrinterConfig[];stations:KitchenStation[];numbering:NumberingSetting[];integrations:IntegrationConfig[];categories:Array<{id:string;name:string;code:string}>}
