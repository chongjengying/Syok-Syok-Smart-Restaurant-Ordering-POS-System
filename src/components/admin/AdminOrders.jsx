import React,{useState} from 'react';import {RefreshCw,Search,X} from 'lucide-react';import {useAdminOperations} from '../../hooks/useAdminOperations';import {cancelOrder,getOrder} from '../../services/order.service';
export default function AdminOrders({canManage=false}){const s=useAdminOperations('orders');const[selected,setSelected]=useState(null);const[detail,setDetail]=useState(null);const[detailError,setDetailError]=useState('');const[reason,setReason]=useState('');const[busy,setBusy]=useState(false);const open=async row=>{setSelected(row);setDetail(null);setDetailError('');const r=await getOrder(row.id);setDetail(r.data);setDetailError(r.error?.message||'');};const cancel=async()=>{if(reason.trim().length<3){setDetailError('Cancellation reason must contain at least 3 characters.');return;}setBusy(true);const r=await cancelOrder(selected.id,reason.trim());setBusy(false);if(r.error){setDetailError(r.error.message);return;}setSelected(null);await s.refresh();};return <section className="space-y-5"><div className="flex justify-between"><div><h1 className="text-2xl font-black">Orders</h1><p className="text-sm text-gray-500">All restaurant orders and financial states.</p></div><button onClick={s.refresh} className="rounded-xl border bg-white p-3"><RefreshCw size={17}/></button></div><div className="flex flex-wrap gap-2"><div className="relative min-w-64 flex-1"><Search className="absolute left-3 top-3 h-4 w-4 text-gray-400"/><input value={s.search} onChange={e=>s.setSearch(e.target.value)} placeholder="Order, table, or staff" className="w-full rounded-xl border bg-white py-2.5 pl-10"/></div><select value={s.filters.status||''} onChange={e=>s.setFilter('status',e.target.value)} className="rounded-xl border bg-white px-3"><option value="">All statuses</option>{['DRAFT','CONFIRMED','PREPARING','READY','SERVED','COMPLETED','CANCELLED','REFUNDED'].map(v=><option key={v}>{v}</option>)}</select><select value={s.filters.paymentStatus||''} onChange={e=>s.setFilter('paymentStatus',e.target.value)} className="rounded-xl border bg-white px-3"><option value="">All payments</option>{['UNPAID','PARTIALLY_PAID','PAID','REFUNDED'].map(v=><option key={v}>{v}</option>)}</select><select value={s.filters.diningMode||''} onChange={e=>s.setFilter('diningMode',e.target.value)} className="rounded-xl border bg-white px-3"><option value="">All modes</option><option value="dine-in">Dine-in</option><option value="takeaway">Takeaway</option></select><input type="date" value={s.filters.dateFrom||''} onChange={e=>s.setFilter('dateFrom',e.target.value)} className="rounded-xl border px-3"/><input type="date" value={s.filters.dateTo||''} onChange={e=>s.setFilter('dateTo',e.target.value)} className="rounded-xl border px-3"/></div>{s.error&&<p className="rounded-xl bg-red-50 p-3 text-red-700">{s.error}</p>}{s.isLoading?<p>Loading orders…</p>:s.rows.length===0?<p className="rounded-xl bg-white p-8 text-center text-gray-400">No orders found.</p>:<div className="overflow-x-auto rounded-2xl bg-white"><table className="w-full text-sm"><thead className="bg-gray-100 text-left text-xs uppercase text-gray-500"><tr><th className="p-3">Order</th><th className="p-3">Mode / Table</th><th className="p-3">Staff</th><th className="p-3">Order Status</th><th className="p-3">Payment</th><th className="p-3 text-right">Total</th><th className="p-3">Created</th></tr></thead><tbody>{s.rows.map(o=><tr key={o.id} onClick={()=>void open(o)} className="cursor-pointer border-t hover:bg-amber-50"><td className="p-3 font-bold">{o.order_number}</td><td className="p-3">{o.dining_mode}<small className="block text-gray-400">{o.table_number||'-'}</small></td><td className="p-3">{o.staff_name||o.user_id}</td><td className="p-3">{o.status}</td><td className="p-3">{o.payment_status}</td><td className="p-3 text-right font-black">RM {Number(o.total).toFixed(2)}</td><td className="p-3 text-xs">{new Date(o.created_at).toLocaleString()}</td></tr>)}</tbody></table><Pager state={s}/></div>}{selected&&<div className="fixed inset-0 z-[70] flex items-center justify-center bg-black/70 p-4"><div className="max-h-[90vh] w-full max-w-2xl overflow-y-auto rounded-2xl bg-white p-6"><div className="flex justify-between"><h2 className="text-xl font-black">{selected.order_number}</h2><button onClick={()=>setSelected(null)}><X/></button></div>{detailError&&<p className="my-3 rounded-xl bg-red-50 p-3 text-red-700">{detailError}</p>}{!detail&&!detailError?<p className="py-8">Loading details…</p>:detail&&<><div className="my-4 grid grid-cols-3 gap-3 text-sm"><div>Status<br/><strong>{detail.status}</strong></div><div>Payment<br/><strong>{detail.paymentStatus}</strong></div><div>Total<br/><strong>RM {detail.total.toFixed(2)}</strong></div></div><h3 className="font-black">Items</h3>{detail.items.map(i=><div key={i.id} className="flex justify-between border-b py-2 text-sm"><span>{i.quantity}× {i.name}</span><strong>RM {i.subtotal.toFixed(2)}</strong></div>)}<h3 className="mt-5 font-black">Timeline</h3>{detail.statusHistory.map(h=><div key={h.id} className="border-l-2 border-[#D4AF37] py-2 pl-3 text-sm"><strong>{h.newStatus}</strong><small className="ml-3 text-gray-400">{new Date(h.changedAt).toLocaleString()}</small><p className="text-xs text-gray-500">{h.notes}</p></div>)}{canManage&&!['COMPLETED','CANCELLED','REFUNDED'].includes(detail.status)&&<div className="mt-5 rounded-xl border border-red-200 p-4"><label className="text-sm font-bold">Cancellation reason<textarea value={reason} onChange={e=>setReason(e.target.value)} className="mt-1 w-full rounded-xl border p-3"/></label><button disabled={busy} onClick={cancel} className="mt-2 rounded-xl bg-red-600 px-4 py-2 font-bold text-white">{busy?'Cancelling…':'Cancel Order'}</button></div>}</>}</div></div>}</section>}
function Pager({state}){const page=Number(state.filters.page||1);return <div className="flex items-center justify-between border-t p-3 text-xs"><span>{state.total} records</span><div className="flex gap-2"><button disabled={page<=1} onClick={()=>state.setFilter('page',page-1)} className="rounded border px-3 py-1 disabled:opacity-40">Previous</button><span className="p-1">Page {page}</span><button disabled={page*25>=state.total} onClick={()=>state.setFilter('page',page+1)} className="rounded border px-3 py-1 disabled:opacity-40">Next</button></div></div>}
