import {useCallback,useEffect,useMemo,useState} from 'react';import {getSystemSettings,saveSystemSettings,uploadSystemLogo} from '../services/systemSettings.service';import type {SystemSettings} from '../types/systemSettings';
export function useSystemSettings(){const[data,setData]=useState<SystemSettings|null>(null);const[draft,setDraft]=useState<SystemSettings|null>(null);const[loading,setLoading]=useState(true);const[saving,setSaving]=useState(false);const[error,setError]=useState('');const[message,setMessage]=useState('');const dirty=useMemo(()=>Boolean(data&&draft&&JSON.stringify(data)!==JSON.stringify(draft)),[data,draft]);
 const load=useCallback(async()=>{setLoading(true);const result=await getSystemSettings();setLoading(false);if(result.error||!result.data){setError(result.error?.message||'Unable to load settings.');return;}setData(result.data);setDraft(structuredClone(result.data));setError('');},[]);
 useEffect(()=>{void load();},[load]);useEffect(()=>{const before=(e:BeforeUnloadEvent)=>{if(dirty){e.preventDefault();e.returnValue='';}};const navigate=(e:Event)=>{if(dirty&&!window.confirm('You have unsaved changes. Discard changes?'))e.preventDefault();};window.addEventListener('beforeunload',before);window.addEventListener('before-admin-navigate',navigate);return()=>{window.removeEventListener('beforeunload',before);window.removeEventListener('before-admin-navigate',navigate);};},[dirty]);
 const save=async()=>{if(!draft)return;if(!window.confirm('Save sensitive system configuration changes?'))return;setSaving(true);setError('');const result=await saveSystemSettings(draft);setSaving(false);if(result.error||!result.data){setError(result.error?.message||'Unable to save settings.');return;}setData(result.data);setDraft(structuredClone(result.data));setMessage('Settings saved successfully.');};
 const uploadLogo=async(file:File)=>{const result=await uploadSystemLogo(file);if(result.error||!result.data){setError(result.error?.message||'Logo upload failed.');return;}setDraft(current=>current?{...current,logoPath:result.data.path,logoUrl:result.data.url}:current);};
 return{data,draft,setDraft,loading,saving,error,message,dirty,load,save,uploadLogo,reset:()=>{if(data)setDraft(structuredClone(data));setError('');setMessage('');}};}
