import { useCallback,useEffect,useState } from 'react';import { getRolePermissionMatrix,saveRolePermissions } from '../services/admin-permission.service';
export function useRolePermissions(){const [data,setData]=useState<any>(null);const [isLoading,setIsLoading]=useState(true);const [busy,setBusy]=useState(false);const [error,setError]=useState('');const [notice,setNotice]=useState('');const refresh=useCallback(async()=>{setIsLoading(true);const r=await getRolePermissionMatrix();setData(r.data);setError(r.error?.message||'');setIsLoading(false);},[]);useEffect(()=>{void refresh();},[refresh]);const save=async(id:string,codes:string[])=>{setBusy(true);setError('');const r=await saveRolePermissions(id,codes);setBusy(false);if(r.error){setError(r.error.message);return false;}setNotice('Permissions updated.');globalThis.dispatchEvent?.(new Event('pos-permissions-changed'));await refresh();return true;};return{data,isLoading,busy,error,notice,save};}
